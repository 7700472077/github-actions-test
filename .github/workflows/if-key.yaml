name: Conditions
# on: [push, pull_request]
on: push

jobs:
  functions:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: teste 1
        continue-on-error: true ## vai executar 
        run: |
          eccho ${{ github.actor }}

      - name: teste 2
        # if: failure() ## vai executar apenas esse step, mesmo que o anterior falhe
        run: |
          eccho ${{ github.repository }}
          
      - name: teste 3
        # if: always()
        run: |
          echo ${{ github.actor }}